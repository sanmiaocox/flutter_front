---
description: 电影交流社区 Flutter 前端项目约定与记忆规则
globs: 
alwaysApply: true
---

# 项目约定（请自动维护）

## 产品与功能
- **本 App 不提供视频/电影播放功能**，仅展示各类图片（海报、头像、活动图等）。所有与"播放""立即观看"等视频相关的入口一律改为"查看详情"等仅展示信息的交互。
- 电影与活动等数据后续通过连接后端 API 获取；当前写死的 Mock 数据仅用于开发与 UI 调试。

## 数据与 Mock
- Mock/死数据必须**集中、易替换**：只放在 `lib/data/` 下，使用与后端约定一致的**模型类**（如 `FeedItem`、`CarouselMovie`、`RankingMovie`、`EventItem`、`MovieCardItem`）。对接后端时只替换数据来源（如从 `HomeMockData` 改为 API 调用），不散落逻辑到页面或组件内部。
- 可复用的 UI 组件只接收上述模型或 DTO，不依赖"写死列表"的实现细节，便于以后直接接 API 返回的数据。

## 目录与分级
- **一级界面**：对应底部导航或顶层路由，每个占一个**一级文件夹**（如 `index`、`activities`、`messages`、`profile`、`login`、`register`、`home`）。
- **二级界面**：从一级入口进入的页面（如搜索、电影详情、活动详情），必须放在**对应一级文件夹的子文件夹**中，例如：
  - 主页相关：`pages/index/` 下含 `index_page.dart` 及子文件夹 `search/`、`movie_detail/`、`event_detail/` 等。
  - 已参加活动相关：`pages/activities/` 下若有活动详情则用 `activities/event_detail/`。
  - 个人中心相关：`pages/profile/` 下若有设置、我的片单等则用 `profile/settings/`、`profile/my_lists/` 等。
- 禁止在 `pages/` 下与一级并列放置本应从属的二级页（如禁止 `pages/search/` 与 `pages/index/` 并列；搜索应位于 `pages/index/search/`）。

lib/
├── main.dart
├── app_theme.dart
├── data/
│   └── home_mock_data.dart          # Mock 集中在此，便于整块替换为 API
├── widgets/
│   ├── feed_card.dart               # 动态卡片组件
│   ├── feed_action_sheet.dart       # 动态操作菜单（收藏/投诉）
│   ├── report_dialog.dart           # 投诉对话框
│   └── ...                          # 其他全局可复用组件
└── pages/
    ├── login/
    │   └── login_page.dart
    ├── register/
    │   └── register_page.dart
    ├── home/
    │   └── home_page.dart            # 仅底部导航容器
    ├── index/                        # 一级：主页
    │   ├── index_page.dart
    │   ├── index_feed_tab.dart
    │   ├── index_ranking_tab.dart
    │   ├── index_events_tab.dart
    │   ├── index_favorites_tab.dart
    │   ├── search/                   # 二级：搜索
    │   │   └── search_page.dart
    │   ├── movie_detail/              # 二级：电影详情
    │   │   └── movie_detail_page.dart
    │   ├── event_detail/             # 二级：活动详情
    │   │   └── event_detail_page.dart
    │   └── feed_detail/              # 二级：动态详情
    │       └── feed_detail_page.dart
    ├── activities/                   # 一级：已参加活动
    │   └── activities_page.dart
    ├── messages/                     # 一级：消息
    │   └── messages_page.dart
    └── profile/                      # 一级：个人中心
        ├── profile_page.dart
        ├── edit_profile_page.dart
        ├── follow_list_page.dart
        ├── settings_page.dart
        └── favorites/                # 二级：收藏夹
            ├── favorites_page.dart
            ├── collection_detail_page.dart
            └── create_collection_page.dart

## 功能实现记录
### 动态功能
- **动态卡片与详情页**：右上角三个小点菜单提供"收藏"和"投诉"功能
  - 点击三个小点弹出底部操作菜单（`FeedActionSheet`）
  - 收藏功能：点击后界面底部显示"收藏成功"提示
  - 投诉功能：点击后弹出文本框对话框（`ReportDialog`），可输入投诉内容（最多200字）并提交，提交后显示"投诉已提交"提示
  - 相关组件：`lib/widgets/feed_action_sheet.dart`、`lib/widgets/report_dialog.dart`

### 电影详情功能
- **电影详情页**：展示完整的电影信息、观影团活动、外部影评链接
  - 顶部：可展开的海报背景 AppBar，带渐变遮罩和评分标签
  - **图片轮播功能**：
    - 支持多张海报图片横向滑动切换
    - 右上角显示当前图片索引（如 1/4）
    - 底部显示圆点指示器
    - 点击图片进入全屏大图查看模式
  - **大图查看功能**（`ImageViewerPage`）：
    - 黑色背景全屏显示
    - 支持手势缩放（0.5x - 4x）
    - 支持左右滑动切换图片
    - 左上角返回按钮
    - 右上角下载按钮，点击保存图片到本地
    - 保存成功后弹窗显示保存路径
    - 底部圆点指示器显示当前图片位置
  - 电影信息卡片：片名、别名、评分、上映日期、集数、类别、地区、语言、导演、演员等
  - 剧情简介：独立卡片展示电影简介
  - 观影团活动：横向滚动卡片列表，展示相关观影团活动（时间、地点、参与人数），点击可跳转到活动详情页
  - 外部影评：链接到豆瓣、知乎、IMDb、烂番茄、TMDB 等平台（使用 `url_launcher` 打开外部链接）
  - 数据模型：`MovieDetail`、`ViewingParty`、`ExternalReviews`（位于 `lib/data/home_mock_data.dart`）
  - 页面位置：`lib/pages/index/movie_detail/movie_detail_page.dart`
  - 图片查看器：`lib/widgets/image_viewer.dart`
  - 设计特点：使用主题色（卡布里蓝、柔和桃）、圆角卡片、阴影效果、渐变背景，界面美观且有新意
  - **动态数据支持**：电影详情页接收 `movieId` 参数，根据ID从数据源获取对应电影信息
    - 已实现三部电影的完整数据：星际穿越(ID:1, 4张海报)、疾速追杀4(ID:2, 3张海报)、彩色梦境(ID:3, 5张海报)
    - 通过 `HomeMockData.getMovieDetailById(movieId)` 获取电影详情
    - 所有入口（动态卡片、榜单、收藏等）点击"查看详情"时传递对应的电影ID
    - 后续对接后端API时，只需将 `getMovieDetailById` 方法改为API调用即可
  - **依赖包**：`path_provider`（获取保存路径）、`http`（下载图片）

### 活动详情功能
- **活动详情页**：展示完整的活动信息、报名和收藏功能
  - 顶部：活动图片背景 AppBar，带渐变遮罩和活动类型标签
  - 活动信息卡片：
    - 时间、地点、发布人（带头像）
    - 已报名人数/总人数、价格
    - 相关电影（可点击跳转到电影详情页）
  - 报名须知：独立卡片展示报名注意事项
  - 活动详情：独立卡片展示活动完整描述
  - 底部悬浮栏：
    - 收藏按钮（左侧，心形图标）
    - 立即报名按钮（右侧，主按钮，点击跳转到报名页面）
    - 名额已满时报名按钮禁用
  - 数据模型：`EventDetail`（位于 `lib/data/home_mock_data.dart`）
  - 页面位置：`lib/pages/index/event_detail/event_detail_page.dart`
  - **动态数据支持**：活动详情页接收 `eventId` 参数，根据ID从数据源获取对应活动信息
    - 已实现四个活动的完整数据：星际穿越观影团(ID:1)、疾速追杀4观影团(ID:2)、彩色梦境观影团(ID:3)、戛纳电影节(ID:4)
    - 通过 `HomeMockData.getEventDetailById(eventId)` 获取活动详情
    - 所有入口（热门活动、电影详情页的观影团卡片）点击时传递对应的活动ID
    - 收藏功能预留了后端API接口，目前显示成功提示
    - 后续对接后端API时，只需将 `getEventDetailById` 方法和按钮回调改为API调用即可

### 活动报名功能
- **活动报名页**（三级页面）：用户填写报名信息并提交
  - 页面位置：`lib/pages/index/event_registration/event_registration_page.dart`
  - **登录检查**：
    - 接收 `isLoggedIn` 参数判断用户是否已登录
    - 未登录时自动弹出提示对话框，引导用户去登录
    - 已登录用户可以正常填写报名信息
  - **活动摘要卡片**：
    - 显示活动缩略图、标题、时间、地点
    - 让用户确认报名的活动信息
  - **报名表单**：
    - 购买票数：使用加减按钮选择（1-10张）
    - 称呼：文本输入框，必填项
    - 手机号：数字输入框，限制11位，必填项
    - 总计金额：根据票数和单价自动计算，免费活动显示"免费"
  - **表单验证**：
    - 称呼不能为空
    - 手机号必须为11位数字
    - 验证失败时显示错误提示
  - **提交流程**：
    1. 点击"确认报名"或"确认并支付"按钮
    2. 验证表单信息
    3. 显示加载状态（按钮显示加载动画）
    4. 模拟网络请求（1秒延迟）
    5. 提交成功后显示成功对话框
    6. 对话框显示报名成功信息和购买票数
    7. 点击确定后返回活动详情页
  - **后端对接准备**：
    - 提交按钮的 `_handleSubmit` 方法中预留了TODO注释
    - 需要提交的数据：`eventId`、`ticketCount`（票数）、`name`（称呼）、`phone`（手机号）
    - 后续替换模拟延迟为真实的API调用
    - 付款功能暂未实现，待后续集成支付SDK
  - **UI设计**：
    - 使用主题色和圆角卡片设计
    - 票数选择器带有加减按钮和大号数字显示
    - 输入框有焦点状态和错误状态的视觉反馈
    - 总计金额使用醒目的大号字体和强调色
    - 底部固定提交按钮，带加载状态

### 个人中心功能
- **个人中心页**：展示用户信息、统计数据、功能入口
  - 页面位置：`lib/pages/profile/profile_page.dart`
  - **用户信息卡片**：头像、用户名、用户ID、个人简介、编辑资料按钮
  - **统计数据**：观影数量、关注数、粉丝数、好友数
  - **功能入口**：收藏夹、动态、我的片单
  - **电影小游戏**：电影问答、猜电影、听歌识影、表情猜片（敬请期待）
  - **后端集成**：已对接用户信息、统计数据、关注关系等API

- **收藏夹功能**（二级页面）：管理电影和活动收藏夹
  - 页面位置：`lib/pages/profile/favorites/`
  - **收藏夹列表页** (`favorites_page.dart`)：
    - 使用 TabBar 区分电影收藏夹和活动收藏夹
    - 展示收藏夹卡片：封面、名称、描述、项目数量、公开性
    - 系统收藏夹显示特殊标签
    - 支持创建新收藏夹
    - 支持下拉刷新
  - **创建收藏夹页** (`create_collection_page.dart`)：
    - 填写收藏夹名称（必填，最多50字符）
    - 填写收藏夹描述（可选，最多200字符）
    - 选择隐私设置（公开/私密）
    - 类型提示卡片（电影收藏夹只能收藏电影，活动收藏夹只能收藏活动）
  - **收藏夹详情页** (`collection_detail_page.dart`)：
    - 可展开的 SliverAppBar 显示封面
    - 收藏夹信息卡片：类型、公开性、描述、项目数量
    - 收藏项列表：显示封面、标题、年份、评分、备注
    - 支持移除收藏项
    - 支持编辑和删除收藏夹（系统收藏夹不可删除）
  - **设计理念**：
    - 电影收藏夹使用红色主题，活动收藏夹使用紫色主题
    - 用户注册时后端自动创建默认电影收藏夹和默认活动收藏夹
    - 收藏夹类型限制：电影收藏夹只能收藏电影，活动收藏夹只能收藏活动
    - 系统收藏夹不能删除，不能修改名称和类型
  - **后端集成**：已对接收藏夹管理、收藏项管理等API
  - **详细文档**：`前端规则文档/收藏夹功能实现说明.md`

- **编辑资料页** (`edit_profile_page.dart`)：修改用户名、头像、个人简介
- **关注列表页** (`follow_list_page.dart`)：查看关注、粉丝、好友列表
- **设置页** (`settings_page.dart`)：账号设置、退出登录

## 规则维护
- 当用户说"记住""关于项目要记住"或类似表述时，请把约定或新事实**补充进本规则文件**，并保持与现有约定一致（产品不提供播放、数据易替换、目录分级）。

# 发布动态界面优化完成

## 优化内容

### 1. UI布局优化

#### 发布按钮优化
- ✅ 将发布按钮改为醒目的 ElevatedButton
- ✅ 使用 `AppTheme.softPeach` 背景色，白色文字
- ✅ 加粗字体，增加视觉权重
- ✅ 添加 padding 使按钮更大更易点击
- ✅ 提交中显示白色加载指示器

#### 文本输入框优化
- ✅ 增加最小行数到 8 行，提供更大的输入空间
- ✅ 添加白色背景和圆角边框
- ✅ 优化内边距，提升输入体验
- ✅ 字符计数器显示在右下角

#### 图片网格优化
- ✅ 改为 2 列布局（原来是 3 列）
- ✅ 设置 `childAspectRatio: 1` 确保图片为正方形
- ✅ 增大删除按钮图标尺寸（18px）
- ✅ 优化间距和圆角

### 2. 功能增强

#### 图片选择提示
- ✅ 当用户选择超过 4 张图片时，自动选择前 N 张
- ✅ 显示提示信息："最多只能选择4张图片，已自动选择前X张"
- ✅ 按钮显示当前已选数量："图片 (2/4)"
- ✅ 达到 4 张时按钮自动禁用

#### 电影/活动关联功能
- ✅ 添加"添加到动态"分组标题
- ✅ 三个操作按钮：图片、电影、活动
- ✅ 每个按钮有独特的颜色和图标
  - 图片：蓝色 (AppTheme.capriBlue)
  - 电影：橙色
  - 活动：绿色
- ✅ 按钮使用浅色背景和边框，视觉统一
- ✅ 禁用状态显示灰色

#### 关联内容展示
- ✅ 电影卡片：显示海报、标题、"已关联电影"标签
- ✅ 活动卡片：显示封面、标题、"已关联活动"标签
- ✅ 卡片可以通过右侧 X 按钮移除
- ✅ 自动加载电影/活动详细信息
- ✅ 图片加载失败时显示占位图标

### 3. 动态卡片图片显示优化

#### FeedCardWidget 图片网格
- ✅ 统一使用 2 列布局
- ✅ 移除了 2 张图片时的特殊处理（原来是 2 列，3-4 张是 3 列）
- ✅ 所有多图情况都使用 2 列，最多显示 4 张
- ✅ 设置 `childAspectRatio: 1` 确保正方形显示
- ✅ 超过 4 张时第 4 张显示 "+N" 提示

## 代码改动

### 文件修改列表

1. **lib/pages/feed/create_feed_page.dart**
   - 添加电影/活动选择状态管理
   - 实现 `_loadMovieInfo()` 和 `_loadEventInfo()` 方法
   - 优化 `_pickImages()` 添加超量提示
   - 添加 `_selectMovie()`、`_selectEvent()` 方法（待实现）
   - 添加 `_removeMovie()`、`_removeEvent()` 方法
   - 重构 UI 布局
   - 添加 `_buildActionButton()` 构建操作按钮
   - 添加 `_buildLinkedMovieCard()` 显示关联电影
   - 添加 `_buildLinkedEventCard()` 显示关联活动

2. **lib/widgets/feed_card_widget.dart**
   - 修改 `_buildImageGrid()` 方法
   - 统一使用 2 列布局
   - 设置 `childAspectRatio: 1`

## UI 效果

### 发布动态页面布局（从上到下）

```
┌─────────────────────────────────┐
│  ← 发布动态          [发布]     │  ← AppBar（蓝色背景，橙色发布按钮）
├─────────────────────────────────┤
│                                 │
│  ┌───────────────────────────┐ │
│  │                           │ │
│  │  分享你的想法...          │ │  ← 文本输入框（8行，白色背景）
│  │                           │ │
│  │                           │ │
│  └───────────────────────────┘ │
│                      1234/2000  │
│                                 │
│  ┌─────────┬─────────┐         │
│  │ [图片1] │ [图片2] │         │  ← 图片网格（2列）
│  └─────────┴─────────┘         │
│  ┌─────────┬─────────┐         │
│  │ [图片3] │ [图片4] │         │
│  └─────────┴─────────┘         │
│                                 │
│  添加到动态                     │  ← 分组标题
│                                 │
│  [📷 图片(2/4)] [🎬 电影] [📅 活动] │  ← 操作按钮
│                                 │
│  ┌───────────────────────────┐ │
│  │ 🎬 已关联电影              │ │
│  │ [海报] 电影标题        [×] │ │  ← 电影卡片（橙色）
│  └───────────────────────────┘ │
│                                 │
│  ┌───────────────────────────┐ │
│  │ 📅 已关联活动              │ │
│  │ [封面] 活动标题        [×] │ │  ← 活动卡片（绿色）
│  └───────────────────────────┘ │
│                                 │
└─────────────────────────────────┘
```

### 动态卡片图片显示

```
单张图片：
┌─────────────────────┐
│                     │
│     [大图显示]      │
│                     │
└─────────────────────┘

2-4张图片（2列布局）：
┌─────────┬─────────┐
│ [图片1] │ [图片2] │
├─────────┼─────────┤
│ [图片3] │ [图片4] │
└─────────┴─────────┘
```

## 待实现功能

### 电影选择对话框
- [ ] 创建电影搜索/选择对话框
- [ ] 支持搜索 TMDB 电影
- [ ] 显示电影列表供用户选择
- [ ] 选择后保存电影 ID 到数据库

### 活动选择对话框
- [ ] 创建活动列表/选择对话框
- [ ] 显示用户创建的活动
- [ ] 显示用户参加的活动
- [ ] 支持搜索活动

## 用户体验改进

### 视觉改进
- ✅ 发布按钮更醒目，不会被忽略
- ✅ 输入框更大，输入体验更好
- ✅ 图片预览更清晰（2列比3列大）
- ✅ 操作按钮分组明确，功能一目了然
- ✅ 关联内容卡片信息丰富，可视化好

### 交互改进
- ✅ 图片超量自动处理，不会让用户困惑
- ✅ 按钮状态清晰（禁用时显示灰色）
- ✅ 关联内容可以随时移除
- ✅ 加载状态有明确的视觉反馈

### 信息反馈
- ✅ 图片数量实时显示
- ✅ 字符计数实时更新
- ✅ 超量选择有友好提示
- ✅ 提交中有加载动画

## 技术细节

### 状态管理
```dart
// 图片选择
final List<XFile> _selectedImages = [];

// 电影关联
int? _selectedMovieId;
TmdbMovie? _selectedMovie;

// 活动关联
int? _selectedEventId;
Event? _selectedEvent;

// 提交状态
bool _isSubmitting = false;
```

### 图片选择逻辑
```dart
Future<void> _pickImages() async {
  if (_selectedImages.length >= 4) {
    // 已达上限，提示用户
    return;
  }

  final images = await _imagePicker.pickMultiImage();
  if (images.isNotEmpty) {
    final remaining = 4 - _selectedImages.length;
    final imagesToAdd = images.take(remaining).toList();
    
    setState(() {
      _selectedImages.addAll(imagesToAdd);
    });
    
    // 如果选择超量，给出提示
    if (images.length > remaining) {
      ScaffoldMessenger.of(context).showSnackBar(
        SnackBar(content: Text('最多只能选择4张图片，已自动选择前${remaining}张')),
      );
    }
  }
}
```

### 图片网格布局
```dart
GridView.builder(
  gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(
    crossAxisCount: 2,        // 固定2列
    crossAxisSpacing: 8,      // 列间距
    mainAxisSpacing: 8,       // 行间距
    childAspectRatio: 1,      // 正方形
  ),
  itemCount: _selectedImages.length,
  itemBuilder: (context, index) {
    // 图片项
  },
)
```

## 测试建议

### 功能测试
1. ✅ 测试选择 1-4 张图片
2. ✅ 测试选择超过 4 张图片（应显示提示）
3. ✅ 测试删除已选图片
4. ✅ 测试输入超过 2000 字（应显示提示）
5. ✅ 测试发布按钮的可见性和可点击性
6. [ ] 测试电影选择功能（待实现）
7. [ ] 测试活动选择功能（待实现）
8. ✅ 测试移除关联的电影/活动

### UI 测试
1. ✅ 验证发布按钮在 AppBar 右侧清晰可见
2. ✅ 验证图片以 2 列显示
3. ✅ 验证操作按钮布局合理
4. ✅ 验证关联卡片显示正确
5. ✅ 验证禁用状态的视觉效果

### 边界测试
1. ✅ 空内容提交（应提示）
2. ✅ 超长内容提交（应提示）
3. ✅ 图片上传失败处理
4. ✅ 网络错误处理
5. ✅ 图片加载失败显示占位图

## 更新日志

### 2026-03-09
- ✅ 优化发布按钮 UI，使用醒目的橙色按钮
- ✅ 增大文本输入框到 8 行
- ✅ 图片网格改为 2 列布局
- ✅ 添加图片超量选择提示
- ✅ 添加电影/活动关联功能 UI
- ✅ 添加关联内容展示卡片
- ✅ 优化动态卡片图片显示为 2 列
- ✅ 所有代码通过编译检查

